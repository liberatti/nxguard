name: CI / develop

on:
  push:
    branches: [ "v*-rc", "v*-fix"]
  pull_request:
    branches: [ "v*-rc", "v*-fix"]

permissions:
  contents: write

jobs:
  code-check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: 3.12
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install flake8 pytest
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Lint
        run: |
          flake8 . --exclude=web --count --select=E9,F63,F7,F82 --show-source --statistics
          flake8 . --exclude=web --count --exit-zero --max-complexity=20 --max-line-length=256 --statistics

  release:
    runs-on: ubuntu-latest
    needs: code-check
    steps:
      - uses: actions/checkout@v4

      - name: Log in to Docker Hub
        if: github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Extract image metadata
        if: github.event_name == 'push'
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: liberatti/nxguard
          tags: |
            type=raw,value=latest

      - name: Pull latest image
        if: github.event_name == 'push'
        run: docker pull liberatti/nxguard:latest || true

      - name: Build and push Docker image
        if: github.event_name == 'push'
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}